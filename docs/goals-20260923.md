# Additional high-level goals

- Simplify complex and lengthy commands
  - write custom plans that handle steps
  - custom plans can be nested, like SPEC macros
- Click on plot to move motor (like `specfe`)
